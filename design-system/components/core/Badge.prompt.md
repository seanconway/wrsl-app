Small uppercase mono status marker. Always carries a word — colour alone never states a status.

```jsx
<Badge tone="live" dot>Live</Badge>
<Badge tone="warn">Battery 12%</Badge>
<Badge tone="red">Red corner</Badge>
```
