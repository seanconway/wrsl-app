Paper-surface container: white, 1px hairline, 9px radius, `--shadow-1`.

```jsx
<Card eyebrow="Mat 3" title="Devices" actions={<IconButton icon="settings" label="Mat settings" />}>
  …
</Card>
```

Inside `.rr-mat` the same component reads as a dark panel with no shadow (the tokens re-point). `selected` adds the 3px lime edge.
