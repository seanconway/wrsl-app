Discrete counters for penalties and advantages — they are counted, never scored.

```jsx
<PenaltyPips label="Pen" count={2} kind="penalty" />
<PenaltyPips label="Adv" count={1} kind="advantage" />
```
