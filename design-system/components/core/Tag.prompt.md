Pill used for filters and division facets in the console (`Adult`, `Blue belt`, `−76kg`).

```jsx
<Tag selected onRemove={() => {}}>Blue belt</Tag>
```

Tag is a filter; Badge is a status. Do not swap them.
