Label + hint/error wrapper shared by every form control.

```jsx
<Field label="Mat number" hint="Displays on the scoreboard header." htmlFor="mat">
  <Input id="mat" value={v} onChange={setV} />
</Field>
```
