Hover/focus label for icon-only controls in the console. Never used on matside surfaces — those are touch or display-only.

```jsx
<Tooltip label="Reset clock"><IconButton icon="rotate-ccw" label="Reset clock" /></Tooltip>
```
