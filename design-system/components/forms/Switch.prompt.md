Immediate on/off for settings that take effect without saving.

```jsx
<Switch checked={sound} onChange={setSound} label="Buzzer on this mat" />
```

Use Checkbox instead when the change is part of a form that gets submitted.
