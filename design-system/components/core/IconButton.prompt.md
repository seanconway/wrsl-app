Square icon-only control for toolbars, rails and row actions. `label` is mandatory.

```jsx
<IconButton icon="rotate-ccw" label="Reset clock" />
<IconButton icon="pause" label="Pause" variant="solid" size="glove" />
```

Use `active` for an armed/selected state (fills lime). Outside a learned position (rail, toolbar) pair it with a Tooltip.
