A single mat in the Console's overview grid — the head table's primary object.

```jsx
<MatTile mat={3} status="live" division="Adult blue · −76kg" clock="2:31"
  red={{ name: 'Silva, M.', points: 4 }} green={{ name: 'Okafor, D.', points: 2 }} queued={5} />
```

A live mat is marked by a 3px lime inset top edge plus the `circle-dot` glyph — never by colour alone.
