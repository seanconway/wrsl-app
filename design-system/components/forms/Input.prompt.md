Single-line text field. Wrap in `Field` for a label.

```jsx
<Input icon="search" placeholder="Search athletes" value={q} onChange={setQ} />
<Input mono value="R-1183" size="sm" />
```

`mono` for IDs, clocks and weights so digits stay aligned.
