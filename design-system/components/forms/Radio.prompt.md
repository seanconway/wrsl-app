Single choice within a named group. Ink dot, not lime — lime is reserved for live state and primary actions.

```jsx
<Radio name="mode" value="gi" checked={m === 'gi'} onChange={setM} label="Gi" />
```
